---
phase: 01-data-prep-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__init__.py
  - models/detect/yolo26n.pt
  - models/detect/yolo26n_ncnn_model/metadata.yaml
  - models/detect/yolo26n_ncnn_model/model_ncnn.py
  - models/detect/yolo26n_ncnn_model/model.ncnn.bin
  - models/detect/yolo26n_ncnn_model/model.ncnn.param
  - models/disease/yolo26n-cls.pt
  - models/disease/yolo26n-cls_ncnn_model/metadata.yaml
  - models/disease/yolo26n-cls_ncnn_model/model_ncnn.py
  - models/disease/yolo26n-cls_ncnn_model/model.ncnn.bin
  - models/disease/yolo26n-cls_ncnn_model/model.ncnn.param
  - reports/.gitkeep
  - scripts/smoke_test_yolo26.py
  - data/zenodo/LICENSE.md
  - data/disease/LICENSE.md
  - data/roboflow/afzaal-bbox-v4/LICENSE.md
  - data/roboflow/matt-lucky-ripeness/LICENSE.md
  - data/roboflow/research-proj-disease/LICENSE.md
  - data/strawdi/LICENSE.md
  - data/osf-ej5qv/LICENSE.md
  - .gitignore
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "Loose pretrained weights no longer exist in repo root"
    - "Pretrained weights are retrievable under models/detect/ and models/disease/"
    - "src/, models/detect/, models/disease/, reports/ directories exist"
    - "Every dataset subfolder has a LICENSE.md that states its license"
    - "data/strawdi/LICENSE.md explicitly calls out non-commercial academic use"
    - "scripts/smoke_test_yolo26.py loads weights from the new paths, not the repo root"
  artifacts:
    - path: "models/detect/yolo26n.pt"
      provides: "Pretrained detector .pt weights in their canonical location"
    - path: "models/detect/yolo26n_ncnn_model/model.ncnn.param"
      provides: "Pretrained detector NCNN export (binary pair) in its canonical location"
    - path: "models/disease/yolo26n-cls.pt"
      provides: "Pretrained classifier .pt weights in canonical location"
    - path: "models/disease/yolo26n-cls_ncnn_model/model.ncnn.param"
      provides: "Pretrained classifier NCNN export in canonical location"
    - path: "src/__init__.py"
      provides: "Python package skeleton root for downstream-phase library code"
    - path: "reports/.gitkeep"
      provides: "Reports dir exists in git even though reports/ is gitignored"
    - path: "data/strawdi/LICENSE.md"
      provides: "Non-commercial academic license constraint for StrawDI_Db1 documented"
    - path: "data/zenodo/LICENSE.md"
      provides: "CC BY 4.0 attribution for Zenodo 6126677"
    - path: "data/disease/LICENSE.md"
      provides: "CC BY 4.0 attribution for Kaggle Afzaal disease dataset"
    - path: "data/roboflow/afzaal-bbox-v4/LICENSE.md"
      provides: "CC BY 4.0 attribution for Roboflow afzaal-bbox-v4"
    - path: "data/roboflow/matt-lucky-ripeness/LICENSE.md"
      provides: "CC BY 4.0 attribution for Roboflow matt-lucky ripeness"
    - path: "data/roboflow/research-proj-disease/LICENSE.md"
      provides: "MIT attribution for Roboflow research-proj disease"
    - path: "data/osf-ej5qv/LICENSE.md"
      provides: "Provenance note marking OSF ej5qv as a partial Kaggle re-host"
  key_links:
    - from: "scripts/smoke_test_yolo26.py"
      to: "models/detect/yolo26n.pt"
      via: "YOLO(...) call with new path"
      pattern: "YOLO\\(.*models/detect/yolo26n\\.pt"
    - from: ".gitignore"
      to: "models/, reports/"
      via: "gitignore rules still match new layout"
      pattern: "^models/|^reports/"
---

<objective>
Create the minimal repo skeleton (`src/`, `models/detect/`, `models/disease/`, `reports/`), migrate the loose pretrained weights and their NCNN exports out of the repo root into `models/`, update the only script that references them (`scripts/smoke_test_yolo26.py`), and author one `LICENSE.md` per dataset folder so downstream phases (and any commercial-deployment audit) can see the license constraint per source.

Purpose: Locks the project layout expected by PROJECT.md / CLAUDE.md. Every downstream plan and phase reads weights from `models/` and writes outputs under `reports/`. The StrawDI non-commercial-academic notice is load-bearing for the eventual commercial deliverable.

Output: Repo structure on disk matching CLAUDE.md "Repo Layout" section, loose weights migrated, one commit-safe `LICENSE.md` per dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-prep-scaffolding/01-CONTEXT.md
@CLAUDE.md
@.gitignore
@scripts/smoke_test_yolo26.py
@requirements.txt

<interfaces>
<!-- Current repo root (inspected 2026-04-23). Loose weights that must move: -->
<!-- /yolo26n.pt                                -> models/detect/yolo26n.pt -->
<!-- /yolo26n-cls.pt                            -> models/disease/yolo26n-cls.pt -->
<!-- /yolo26n_ncnn_model/                       -> models/detect/yolo26n_ncnn_model/ -->
<!-- /yolo26n-cls_ncnn_model/                   -> models/disease/yolo26n-cls_ncnn_model/ -->

<!-- Script references to loose weights (grepped with grep -rn "yolo26n" scripts/): -->
<!-- scripts/smoke_test_yolo26.py:50    det = YOLO("yolo26n.pt") -->
<!-- scripts/smoke_test_yolo26.py:64    cls = YOLO("yolo26n-cls.pt") -->
<!-- Update both to use models/detect/yolo26n.pt and models/disease/yolo26n-cls.pt respectively. -->

<!-- Dataset licenses (authoritative — pulled from data.yaml files and research notes): -->
<!-- data/zenodo/strawberries/         CC BY 4.0         Pastell, Lemsalu, Bloch 2022, Zenodo DOI 10.5281/zenodo.6126677 -->
<!-- data/disease/                     CC BY 4.0         Kaggle Afzaal — usmanafzaal/strawberry-disease-detection-dataset -->
<!-- data/roboflow/afzaal-bbox-v4/     CC BY 4.0         Roboflow strawberry-disease/strawberry-disease-detection-dataset v4 -->
<!-- data/roboflow/matt-lucky-ripeness/ CC BY 4.0         Roboflow matt-lucky-f7mch/strawberry-k4gtp v1 -->
<!-- data/roboflow/research-proj-disease/ MIT            Roboflow research-proj/strawberry-diseases-detection v1 -->
<!-- data/strawdi/extracted/StrawDI_Db1/ Non-commercial academic    Perez-Borrero et al, U. Huelva (StrawDI_Db1) -->
<!-- data/osf-ej5qv/                   Partial Kaggle re-host; treat as Kaggle-derivative (polygons stripped, classes reorganized); NOT a training source -->

<!-- .gitignore (already covers new dirs — verified before this plan): -->
<!-- models/       -> ignores entire models/ subtree -->
<!-- reports/      -> ignores entire reports/ subtree -->
<!-- *.pt, *_ncnn_model/ -> catches migrated artifacts even outside models/ -->
<!-- LICENSE.md under data/ subdirs is NOT gitignored (data/ is ignored, so add exceptions). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton directories and migrate loose weights</name>
  <files>
    src/__init__.py
    models/detect/yolo26n.pt
    models/detect/yolo26n_ncnn_model/ (entire directory, 4 files + __pycache__ stripped)
    models/disease/yolo26n-cls.pt
    models/disease/yolo26n-cls_ncnn_model/ (entire directory)
    reports/.gitkeep
    scripts/smoke_test_yolo26.py
  </files>
  <read_first>
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/.gitignore (confirm models/, reports/ already covered)
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/scripts/smoke_test_yolo26.py (two call sites to update — lines 50 and 64)
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/CLAUDE.md Repo Layout section
  </read_first>
  <action>
    1. Create directories: `mkdir -p src models/detect models/disease reports`
    2. Create empty package marker: `touch src/__init__.py`
    3. Create reports gitkeep (so the dir exists in the repo even while reports/ is gitignored):
       `printf '' > reports/.gitkeep`
       Then add a negation exception to .gitignore so .gitkeep files in reports/ are tracked:
       Append to .gitignore a new block after the `reports/` line:
       ```
       !reports/.gitkeep
       ```
       (Do NOT remove the `reports/` ignore rule. The negation line targets the sentinel file only.)
    4. Move pretrained weights from repo root into models/ (use `git mv` if any of these are tracked; otherwise plain `mv`. Verify with `git ls-files | grep -E '^yolo26n'` first — expected: none, since *.pt and *_ncnn_model/ are gitignored):
       ```
       mv yolo26n.pt                 models/detect/yolo26n.pt
       mv yolo26n-cls.pt             models/disease/yolo26n-cls.pt
       mv yolo26n_ncnn_model         models/detect/yolo26n_ncnn_model
       mv yolo26n-cls_ncnn_model     models/disease/yolo26n-cls_ncnn_model
       ```
    5. Strip stray bytecode caches inside moved NCNN dirs so the move is clean:
       ```
       rm -rf models/detect/yolo26n_ncnn_model/__pycache__
       rm -rf models/disease/yolo26n-cls_ncnn_model/__pycache__
       ```
    6. Update `scripts/smoke_test_yolo26.py` so YOLO() loads the migrated weights instead of re-downloading to cwd:
       - Line 50: change `det = YOLO("yolo26n.pt")` to `det = YOLO("models/detect/yolo26n.pt")`
       - Line 64: change `cls = YOLO("yolo26n-cls.pt")` to `cls = YOLO("models/disease/yolo26n-cls.pt")`
       - Also fix the stale dataset path on line 107: `data_dir = Path("/Users/adit/Desktop/Pyranthus/products/multi-spectral/data/zenodo/strawberries/training")` → `data_dir = Path(__file__).resolve().parent.parent / "data/zenodo/strawberries/training"` (repo-relative, under this project, not the legacy `multi-spectral/` folder).
    7. Confirm .gitignore still covers the new layout. It already has `models/`, `reports/`, `*.pt`, `*_ncnn_model/` — no further change beyond the `!reports/.gitkeep` negation from step 3.
  </action>
  <verify>
    <automated>
      test -f src/__init__.py && \
      test -f models/detect/yolo26n.pt && \
      test -f models/detect/yolo26n_ncnn_model/model.ncnn.param && \
      test -f models/detect/yolo26n_ncnn_model/model.ncnn.bin && \
      test -f models/disease/yolo26n-cls.pt && \
      test -f models/disease/yolo26n-cls_ncnn_model/model.ncnn.param && \
      test -f reports/.gitkeep && \
      ! test -e yolo26n.pt && \
      ! test -e yolo26n-cls.pt && \
      ! test -e yolo26n_ncnn_model && \
      ! test -e yolo26n-cls_ncnn_model && \
      grep -q 'models/detect/yolo26n\.pt' scripts/smoke_test_yolo26.py && \
      grep -q 'models/disease/yolo26n-cls\.pt' scripts/smoke_test_yolo26.py && \
      ! grep -q 'multi-spectral' scripts/smoke_test_yolo26.py && \
      .venv/bin/python scripts/smoke_test_yolo26.py 2>&1 | tee /tmp/smoke_out.txt | grep -q 'smoke test complete' && \
      grep -q '\[OK\]  load' /tmp/smoke_out.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - All six `test -f` / `! test -e` conditions above succeed (loose weights gone from root; canonical copies present under models/).
    - `grep 'models/detect/yolo26n\.pt' scripts/smoke_test_yolo26.py` returns the updated YOLO() call on line 50.
    - `grep 'models/disease/yolo26n-cls\.pt' scripts/smoke_test_yolo26.py` returns the updated YOLO() call on line 64.
    - `grep 'multi-spectral' scripts/smoke_test_yolo26.py` returns no matches (stale path eliminated).
    - Running `.venv/bin/python scripts/smoke_test_yolo26.py` prints `smoke test complete` and contains at least one `[OK]  load` line for both detect and cls (weights load from the new paths).
    - `git status --porcelain | grep -E '^(M|A|R)' | wc -l` is > 0 (there is tracked change to commit).
    - `.gitignore` contains `!reports/.gitkeep` on its own line.
  </acceptance_criteria>
  <done>
    Skeleton dirs exist, loose weights migrated under models/ with no copies left in root, scripts/smoke_test_yolo26.py runs to completion against the new paths, .gitignore still suppresses models/ and reports/ while allowing the reports/.gitkeep sentinel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author per-dataset LICENSE.md files</name>
  <files>
    data/zenodo/LICENSE.md
    data/disease/LICENSE.md
    data/roboflow/afzaal-bbox-v4/LICENSE.md
    data/roboflow/matt-lucky-ripeness/LICENSE.md
    data/roboflow/research-proj-disease/LICENSE.md
    data/strawdi/LICENSE.md
    data/osf-ej5qv/LICENSE.md
    .gitignore
  </files>
  <read_first>
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/data/roboflow/afzaal-bbox-v4/data.yaml (for license + project url)
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/data/roboflow/matt-lucky-ripeness/data.yaml
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/data/roboflow/research-proj-disease/data.yaml
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/.planning/research/2026-04-21-dataset-inventory-and-splits.md (§2 + §4 — canonical license + provenance statements for each dataset)
    - /Users/adit/Desktop/Pyranthus/products/strawb-analysis/.gitignore (must add negation rules so LICENSE.md under data/ is tracked)
  </read_first>
  <action>
    1. Add `.gitignore` exceptions so LICENSE.md files under the gitignored `data/` tree remain tracked. Append these lines AFTER the existing `data/` rule (keep that rule intact):
       ```
       !data/**/LICENSE.md
       ```
       This pattern (Git negation over nested path) permits all LICENSE.md files at any depth under data/.
    2. Write 7 LICENSE.md files. Each file MUST contain: dataset name, license identifier (SPDX-style where applicable), source URL, citation string (if any), commercial-use disposition (one line), and any usage constraints.

       Use these exact bodies (one-shot; do not paraphrase):

       **data/zenodo/LICENSE.md:**
       ```
       # License — Zenodo 6126677 (Strawberries dataset for object detection)

       - **License:** CC BY 4.0
       - **SPDX:** CC-BY-4.0
       - **Source:** https://zenodo.org/record/6126677
       - **DOI:** 10.5281/zenodo.6126677
       - **Citation:** Pastell, Matti; Lemsalu, Mikk; Bloch, Vladislav (2022). Strawberries dataset for object detection. Zenodo.
       - **Commercial use:** Permitted with attribution.
       - **Usage constraints:** Attribution required in deliverable README. No further restrictions.

       Original purpose: "created to support strawberry harvesting robot development." Three classes: ripe, unripe, peduncle. YOLO bbox labels, 813 images (654 train + 159 val).
       ```

       **data/disease/LICENSE.md:**
       ```
       # License — Kaggle Afzaal Strawberry Disease Detection Dataset

       - **License:** CC BY 4.0
       - **SPDX:** CC-BY-4.0
       - **Source:** https://www.kaggle.com/datasets/usmanafzaal/strawberry-disease-detection-dataset
       - **Citation:** Afzaal, Usman (Kaggle, 2021). Strawberry Disease Detection Dataset.
       - **Commercial use:** Permitted with attribution.
       - **Usage constraints:** Attribution required in deliverable README.

       7 disease classes (angular_leafspot, anthracnose_fruit_rot, blossom_blight, gray_mold, leaf_spot, powdery_mildew_fruit, powdery_mildew_leaf). LabelMe polygon JSON labels, 2500 unique images across train/val/test.
       ```

       **data/roboflow/afzaal-bbox-v4/LICENSE.md:**
       ```
       # License — Roboflow afzaal-bbox-v4

       - **License:** CC BY 4.0 (inherited from Kaggle Afzaal source)
       - **SPDX:** CC-BY-4.0
       - **Source:** https://universe.roboflow.com/strawberry-disease/strawberry-disease-detection-dataset/dataset/4
       - **Provenance:** Community re-host of Kaggle Afzaal with polygon→bbox conversion and augmentation. Not an independent image source; treat as Kaggle-derivative for split-isolation purposes (see `data/osf-ej5qv/LICENSE.md` for overlap discussion).
       - **Commercial use:** Permitted with attribution.
       - **Usage constraints:** Lossy 280×280 resize vs Kaggle's 419×419 source. Attribution required.

       4898 images (train 3426 / val 988 / test 484), 7 disease classes, YOLO bbox.
       ```

       **data/roboflow/matt-lucky-ripeness/LICENSE.md:**
       ```
       # License — Roboflow matt-lucky-f7mch/strawberry-k4gtp

       - **License:** CC BY 4.0
       - **SPDX:** CC-BY-4.0
       - **Source:** https://universe.roboflow.com/matt-lucky-f7mch/strawberry-k4gtp/dataset/1
       - **Provenance:** Community-contributed dataset. No upstream paper or capture-rig documentation. Treat as independent from Zenodo (zero MD5 collisions in 50×50 sample).
       - **Commercial use:** Permitted with attribution.
       - **Usage constraints:** Attribution required. Provenance unverified — spot-inspect before weighting heavily in training (see research/2026-04-21-dataset-inventory-and-splits.md §8 action item).

       2535 images (train 2388 / val 119 / test 28), 2 classes (ripe, unripe), 640×640, YOLO bbox.
       ```

       **data/roboflow/research-proj-disease/LICENSE.md:**
       ```
       # License — Roboflow research-proj/strawberry-diseases-detection

       - **License:** MIT
       - **SPDX:** MIT
       - **Source:** https://universe.roboflow.com/research-proj/strawberry-diseases-detection/dataset/1
       - **Provenance:** Community-contributed. 7 Afzaal disease classes + 3 explicit healthy classes (Healthy Fruit, Healthy Leaf, Healthy Flower). May be Afzaal-derivative (class list overlap) — treat as Kaggle-derivative for split-isolation purposes.
       - **Commercial use:** Permitted with attribution per MIT terms.
       - **Usage constraints:** None beyond MIT attribution. Primary value for this project: Healthy Fruit bbox class (alternative to synthesized healthy per Pitfall #2).

       2757 images (train 1882 / val 553 / test 322), 10 classes, 640×640, YOLO bbox.
       ```

       **data/strawdi/LICENSE.md:**
       ```
       # License — StrawDI_Db1 (Huelva)

       - **License:** NON-COMMERCIAL ACADEMIC USE ONLY
       - **SPDX:** (no standard SPDX identifier; custom academic license)
       - **Source:** https://strawdi.github.io/
       - **Citation:** Perez-Borrero, I.; Marin-Santos, D.; Gegundez-Arias, M.E.; Cortes-Ancos, E. (2020). A fast and accurate deep learning method for strawberry instance segmentation. Computers and Electronics in Agriculture.
       - **Commercial use:** **NOT PERMITTED.** This dataset is restricted to non-commercial academic research.
       - **Usage constraints (LOAD-BEARING):**
         - MUST NOT be redistributed as part of a commercial product.
         - MUST NOT be used to train any model weights that ship in the commercial deliverable.
         - MAY be used for ablation studies, research, and internal comparison only.
         - Any detector weights trained with StrawDI data must be clearly segregated from the production weights shipped to the client.

       3100 instance-seg masks (train 2800 / val 100 / test 200), single class (strawberry), 1008×756 PNG masks.

       **CRITICAL:** Before any commercial deployment, verify that no production model checkpoint has been trained on StrawDI data. If unsure, retrain without StrawDI.
       ```

       **data/osf-ej5qv/LICENSE.md:**
       ```
       # License — OSF ej5qv (partial Kaggle re-host)

       - **License:** Inherits CC BY 4.0 for disease classes (Kaggle Afzaal derivative); ripe/unripe classes have **UNVERIFIED PROVENANCE**.
       - **SPDX:** CC-BY-4.0 (disease classes only)
       - **Source:** https://osf.io/ej5qv/
       - **Provenance:** Inspection (see research/2026-04-21-dataset-inventory-and-splits.md §4) shows ~92% basename overlap with Kaggle Afzaal disease dataset and 16/200 byte-identical MD5 collisions on sampled pairs. Disease classes are a classification-reorganized re-host of Kaggle (polygons stripped, classes as subdirs). Ripe/unripe classes have unknown origin — not in the OSF README, not referenced in any prior research source.
       - **Commercial use:** Disease classes: permitted with Kaggle attribution. Ripe/unripe classes: **DO NOT USE** until provenance is verified.
       - **Usage constraints (LOAD-BEARING):**
         - Directory name ` angular/` has a leading space (archive bug). Handle explicitly.
         - **DO NOT mix OSF disease classes into training alongside Kaggle Afzaal** — same source images, split leakage risk.
         - Treated as a non-training source in this project: use Kaggle polygon form as the canonical disease dataset.

       2967 files, 9 class subdirs.
       ```

    3. Make sure each LICENSE.md file ends with a trailing newline (POSIX).
  </action>
  <verify>
    <automated>
      test -f data/zenodo/LICENSE.md && \
      test -f data/disease/LICENSE.md && \
      test -f data/roboflow/afzaal-bbox-v4/LICENSE.md && \
      test -f data/roboflow/matt-lucky-ripeness/LICENSE.md && \
      test -f data/roboflow/research-proj-disease/LICENSE.md && \
      test -f data/strawdi/LICENSE.md && \
      test -f data/osf-ej5qv/LICENSE.md && \
      grep -q 'NON-COMMERCIAL ACADEMIC USE ONLY' data/strawdi/LICENSE.md && \
      grep -q 'CC BY 4.0' data/zenodo/LICENSE.md && \
      grep -q 'CC BY 4.0' data/disease/LICENSE.md && \
      grep -q 'MIT' data/roboflow/research-proj-disease/LICENSE.md && \
      grep -q 'UNVERIFIED PROVENANCE' data/osf-ej5qv/LICENSE.md && \
      grep -q '!data/\*\*/LICENSE\.md' .gitignore && \
      git check-ignore -q data/strawdi/LICENSE.md; [ $? -eq 1 ] && \
      git check-ignore -q data/zenodo/LICENSE.md; [ $? -eq 1 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - All 7 LICENSE.md files exist and are non-empty.
    - `grep 'NON-COMMERCIAL ACADEMIC USE ONLY' data/strawdi/LICENSE.md` returns at least one match (StrawDI constraint spelled out verbatim).
    - `grep 'DO NOT mix OSF disease classes' data/osf-ej5qv/LICENSE.md` returns a match (leakage warning present).
    - `.gitignore` contains the exact line `!data/**/LICENSE.md`.
    - `git check-ignore -q <path>` returns 1 (not ignored) for every LICENSE.md listed above. If any return 0 (ignored), the .gitignore negation is wrong and must be fixed.
  </acceptance_criteria>
  <done>
    Every dataset folder has a LICENSE.md that downstream agents (and the commercial-deployment audit) can read to determine license posture. StrawDI's non-commercial constraint is documented in a load-bearing way. OSF's leakage risk is documented so the MANIFEST generator (Plan 02) has a reference to cite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-root → models/ migration | File-system moves; failure leaves broken paths. No network or untrusted input. |
| .gitignore edits | Wrong negation pattern could accidentally track large binary artifacts. |
| script path rewrite | Wrong relative path leaves smoke test broken on first run after merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | weight migration (mv from root to models/) | mitigate | Verify post-move checksums implicitly by re-running smoke test (loads weights from new path; ultralytics validates internal structure on load). If load fails, the task FAILs. |
| T-01-02 | Information Disclosure | .gitignore negation | mitigate | Use narrowest possible negation (`!data/**/LICENSE.md`, `!reports/.gitkeep`) — cannot accidentally un-ignore images or *.pt. Verify with `git check-ignore` per LICENSE.md and by `git status --porcelain` showing no unexpected tracked additions. |
| T-01-03 | Repudiation | license claims in LICENSE.md files | accept | License strings sourced from the canonical `data.yaml` files shipped with each Roboflow export and from prior research notes. If upstream license changes, LICENSE.md will go stale — but git history preserves what was asserted at time of commit. |
| T-01-04 | Denial of Service | smoke test path change | mitigate | Post-change smoke test is part of the automated verify block; any regression fails the task. |
</threat_model>

<verification>
After this plan completes:
- `ls models/detect/ models/disease/ src/ reports/` lists the expected skeleton on disk.
- `ls yolo26n*.pt yolo26n*_ncnn_model 2>/dev/null` returns nothing (loose weights gone from root).
- `.venv/bin/python scripts/smoke_test_yolo26.py` runs end-to-end, prints at least the detect and cls `[OK]  load` lines, and exits 0.
- Every dataset subfolder has a LICENSE.md; `git check-ignore` confirms they will be tracked at commit time.
</verification>

<success_criteria>
- Skeleton dirs `src/`, `models/detect/`, `models/disease/`, `reports/` exist.
- Pretrained weights + NCNN exports live under `models/detect/` and `models/disease/` only (no copies in repo root).
- `scripts/smoke_test_yolo26.py` loads weights from the migrated paths and the stale `multi-spectral/` dataset path is replaced with a repo-relative path.
- `data/strawdi/LICENSE.md` explicitly states non-commercial academic use and the commercial-deployment constraint.
- Seven LICENSE.md files are present, tracked (not gitignored), and each names the dataset's license + source URL.
- `.gitignore` contains `!data/**/LICENSE.md` and `!reports/.gitkeep` and still excludes everything else under `data/`, `models/`, `reports/`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-prep-scaffolding/01-01-SUMMARY.md` capturing:
- The skeleton layout on disk
- Migration audit (src path -> dst path, one row per artifact)
- Key smoke-test output lines (load OK + inference OK)
- Any .gitignore edits made beyond the two listed
- Any dataset LICENSE.md content decisions that deviated from the spec above (should be none)
</output>
